@model Entities.EDMX.Consorcio

@{
    ViewBag.Title = "Eliminar";
}

@*
    @{Html.RenderPartial("~/Views/Partials/pv_breadcrump.cshtml");}
*@
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center mb-1">

    <h2>Eliminar Consorcio</h2>

    <a onclick="volver()" class="btn btn-success">Volver</a>

</div>

<div>
    <p>Confirme la eliminación del consorcio seleccionado</p>
    <hr />


    <article class="d-block text-center align-middle">

        <article class="border rounded bg-light mb-5 mt-3">
            <div class="card-body">
                <h3 class="card-title m-4">¿Desea eliminar el consorcio "@Model.Nombre"?</h3>
                <div class="card-text">

                    <p class="text-danger font-weight-bold">
                        Se borrará el consorcio y todos sus datos relacionados.
                    </p>

                    <div class="m-4">
                        <form action="/Consorcio/Eliminar" method="post">
                            <input type="submit" value="Eliminar" class="btn btn-danger" />
                            <input type="hidden" name="IdConsorcio" value="@Model.IdConsorcio" />
                            @Html.ActionLink("Cancelar", "Listado", "Consorcio", null, new { @class = "btn btn-success" })
                        </form>
                    </div>

                </div>
            </div>
        </article>
    </article>

</div>

@Scripts.Render("~/bundles/custom")
